<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Book Ticket - Musango Express</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <header>
    <div class="container">
      <img src="/images/logo.png" alt="Musango Express Logo" class="logo">
      <nav>
        <ul>
          <li><a href="/">Home</a></li>
          <li><a href="/services">Services</a></li>
          <li><a href="/destinations">Destinations</a></li>
          <li><a href="/contacts">Contacts</a></li>
          <li><a href="/about">About Us</a></li>
        </ul>
      </nav>
    </div>
  </header>

  <main class="content">
    <h1>Book Your Ticket to <%= destination %></h1>
    
    <!-- Display error message if any -->
    <% if (error) { %>
      <div class="error-message">
        <p><%= error %></p>
      </div>
    <% } %>

    <form action="/book" method="POST">
      <input type="hidden" name="destination" value="<%= destination %>">
      
      <div class="form-group">
        <label for="name">Full Name:</label>
        <input type="text" id="name" name="name" value="<%= formData.name || '' %>" required>
      </div>
      
      <div class="form-group">
        <label for="age">Age:</label>
        <input type="number" id="age" name="age" value="<%= formData.age || '' %>" required>
      </div>
      
      <div class="form-group">
        <label for="contact">Contact:</label>
        <input type="text" id="contact" name="contact" value="<%= formData.contact || '' %>" required>
      </div>
      
      <div class="form-group">
        <label for="date">Date:</label>
        <input type="date" id="date" name="date" value="<%= formData.date || '' %>" required>
      </div>
      
      <div class="form-group">
        <label for="time">Time:</label>
        <select id="time" name="time" required>
          <option value="">Select a time</option>
          <option value="06:00 AM" <%= formData.time === '06:00 AM' ? 'selected' : '' %>>06:00 AM</option>
          <option value="09:00 AM" <%= formData.time === '09:00 AM' ? 'selected' : '' %>>09:00 AM</option>
          <option value="12:00 PM" <%= formData.time === '12:00 PM' ? 'selected' : '' %>>12:00 PM</option>
          <option value="03:00 PM" <%= formData.time === '03:00 PM' ? 'selected' : '' %>>03:00 PM</option>
          <option value="06:00 PM" <%= formData.time === '06:00 PM' ? 'selected' : '' %>>06:00 PM</option>
          <option value="09:00 PM" <%= formData.time === '09:00 PM' ? 'selected' : '' %>>09:00 PM</option>
        </select>
      </div>
      
      <div class="form-group">
        <label for="busSerial">Bus Serial Number:</label>
        <select id="busSerial" name="busSerial" required>
          <option value="">Select a bus</option>
          <% for (let i = 1; i <= 5; i++) { %>
            <option value="<%= destination.slice(0, 3).toUpperCase() %>-00<%= i %>"
              <%= formData.busSerial === `${destination.slice(0, 3).toUpperCase()}-00${i}` ? 'selected' : '' %>>
              <%= destination.slice(0, 3).toUpperCase() %>-00<%= i %>
            </option>
          <% } %>
        </select>
      </div>
      
      <div class="form-group">
        <button type="submit">Submit</button>
      </div>
    </form>
  </main>

  <footer>
    <p>&copy; 2023 Musango Express. All rights reserved.</p>
  </footer>
</body>
</html>